let temperatura = 32;

if(temperatura < 10) {
    console.log(`Hace mucho frío, la temperatura es de ${temperatura}°C.`);
} else if(temperatura >= 10 && temperatura <=25) {
    console.log(`El clima está templado con ${temperatura}°C.`);
} else {
    console.log(`Hace mucho calor, la temperatura es de ${temperatura}°C.`);
}